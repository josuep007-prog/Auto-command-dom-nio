{
  "name": "gerador-importacao-dominio",
  "version": "27.0.0",
  "private": true,
  "description": "Gerador de arquivos de importacao do Dominio (RPA e Lancamentos) — roda offline, num arquivo HTML so",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verificar-vendor.mjs",
    "dev": "http-server src -p 4173 -c-1 --silent",
    "test": "npm run build && playwright test",
    "test:ui": "npm run build && playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "http-server": "^14.1.1"
  }
}
